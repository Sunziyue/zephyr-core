<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="TBpSaleposT">

    <sql id="table_name">
        t_mt_order_t
    </sql>

    <sql id="columns">
        drpid, mktid, ymd,
        bill, id, payid, coinid,
        no_card, no_bank, num_pay,
        rate_bk, value_pay, trade_time,
        dealerno, coupon_bill, cpid,
        termino, payid_port, parentcode,
        bankcode, id_old, payid_old,
        online_return
    </sql>

    <sql id="values">
        #{drpid,jdbcType=CHAR}, #{mktid,jdbcType=CHAR}, #{ymd,jdbcType=INTEGER},
        #{bill,jdbcType=CHAR}, #{id,jdbcType=INTEGER}, #{payid,jdbcType=VARCHAR}, #{coinid,jdbcType=CHAR},
        #{no_card,jdbcType=CHAR}, #{no_bank,jdbcType=VARCHAR}, #{num_pay,jdbcType=DECIMAL},
        #{rate_bk,jdbcType=DECIMAL}, #{value_pay,jdbcType=DECIMAL}, #{trade_time,jdbcType=TIMESTAMP},
        #{dealerno,jdbcType=VARCHAR}, #{coupon_bill,jdbcType=VARCHAR}, #{cpid,jdbcType=VARCHAR},
        #{termino,jdbcType=VARCHAR}, #{payid_port,jdbcType=VARCHAR}, #{parentcode,jdbcType=VARCHAR},
        #{bankcode,jdbcType=VARCHAR}, #{id_old,jdbcType=INTEGER}, #{payid_old,jdbcType=VARCHAR},
        #{online_return,jdbcType=VARCHAR}
    </sql>

    <insert id="insert" parameterType="TBpSaleposT">
        INSERT INTO
        <include refid="table_name"/>
        (<include refid="columns"/>)
        VALUES
        (<include refid="values"/>)
    </insert>
</mapper>
